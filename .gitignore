# Dump files (large binary files)
dumps/*.dmp

# Symbol files (large, downloadable)
symbols/*

# Reports (can be regenerated)
reports/*.txt
reports/*.json

# Tools binary (can be re-downloaded via install-tools.ps1)
tools/*

# Test electron (can be re-downloaded)
test-electron
